describe('Dashboard', () => {
  it('can navigate to the dashboard', () => {
    expect(true).to.equal(true);
  });

  it('can navigate to the dashboard', () => {
    
  });
});


//cy.get('[data-cy="submit"]').click()	

// todo move interfaces into another file
// todo should the prisma query live here or somewhere else. 
// todo i have a lot of divs. necessary? 
